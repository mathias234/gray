let arr = [0, 1, 2, 3, 4, 5];

let idx = 0;

let sum = 0;

while idx < array::len(arr) {
    sum += arr[idx];
    idx += 1;
}

assert_eq(sum, 15);

let sum = 0;

for v in arr {
    sum += v;
}

assert_eq(sum, 15);